MODE = "dev"



[dev]

HOST        = "0.0.0.0"
PORT        = 9999
SECRET_KEY  = ""
UTC_OFFSET  = -6  # For Oklahoma, -5 for Washington DC

DB_HOST         = ""
DB_PORT         = 9999
DB_NAME         = ""
DB_USER         = ""
DB_PASS         = ""
DB_TDS_VERSION  = ""

LOG_LEVEL           = "DEBUG"    # DEBUG -> INFO -> WARNING -> ERROR -> CRITICAL
LOG_FORMATTER       = "json"   # "json" (outputs .jsonl) or "simple" (outputs .log)
LOG_FILE_SIZE       = 3_000_000  # size of single log file in bytes
LOG_FILE_ROTATION   = 5          # the maximum number of log files in rotation until deletion

# Exclude ".json" or ".jsonl" extensions, they will be added if needed
LOG_FILE_PATH       = "./.logs/django"

LDAP_SERVER_URI = ""
LDAP_AUTH_BIND_DN = ""
LDAP_AUTH_BIND_PASS = ""
LDAP_SEARCH_OU = ""

ALLOWED_HOSTS = [
    "localhost",
    "0.0.0.0",
    "127.0.0.1",
]


[test]

HOST        = "0.0.0.0"
PORT        = 9999
SECRET_KEY  = ""
UTC_OFFSET  = -6  # For Oklahoma, -5 for Washington DC

DB_HOST         = ""
DB_PORT         = 9999
DB_NAME         = ""
DB_USER         = ""
DB_PASS         = ""
DB_TDS_VERSION  = ""

LOG_LEVEL           = "DEBUG"    # DEBUG -> INFO -> WARNING -> ERROR -> CRITICAL
LOG_FORMATTER       = "simple"   # "json" (outputs .jsonl) or "simple" (outputs .log)
LOG_FILE_SIZE       = 3_000_000  # size of single log file in bytes
LOG_FILE_ROTATION   = 5          # the maximum number of log files in rotation until deletion

# Exclude ".json" or ".jsonl" extensions, they will be added if needed
LOG_FILE_PATH       = "./.logs/django.log"

LDAP_SERVER_URI = ""
LDAP_AUTH_BIND_DN = ""
LDAP_AUTH_BIND_PASS = ""
LDAP_SEARCH_OU = ""

ALLOWED_HOSTS = [
    "localhost",
    "0.0.0.0",
    "127.0.0.1",
]


[prod]

HOST        = "0.0.0.0"
PORT        = 9999
SECRET_KEY  = ""
UTC_OFFSET  = -6  # For Oklahoma, -5 for Washington DC

DB_HOST         = ""
DB_PORT         = 9999
DB_NAME         = ""
DB_USER         = ""
DB_PASS         = ""
DB_TDS_VERSION  = ""

LOG_LEVEL           = "DEBUG"    # DEBUG -> INFO -> WARNING -> ERROR -> CRITICAL
LOG_FORMATTER       = "simple"   # "json" (outputs .jsonl) or "simple" (outputs .log)
LOG_FILE_SIZE       = 3_000_000  # size of single log file in bytes
LOG_FILE_ROTATION   = 5          # the maximum number of log files in rotation until deletion

# Exclude ".json" or ".jsonl" extensions, they will be added if needed
LOG_FILE_PATH       = "./.logs/django.log"

LDAP_SERVER_URI = ""
LDAP_AUTH_BIND_DN = ""
LDAP_AUTH_BIND_PASS = ""
LDAP_SEARCH_OU = ""

ALLOWED_HOSTS = [
    "localhost",
    "0.0.0.0",
    "127.0.0.1",
]